[package]
name = "gitkat-rewrite"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
gix = { version = "0.76", features = ["revision", "tree-editor"] }
gix-object = "0.53"
globset = "0.4"
regex = "1.10"
