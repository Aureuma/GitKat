# Installation

## Requirements

- Git
- Rust toolchain (cargo)
- `git-filter-repo` (optional, for `gk verify-rewrite`)

```sh
cargo install --path .
# or for local builds
cargo build --release
./target/release/gk --help
```
